# Chat Surfaces

Use this guidance for WhatsApp, Slack, Discord, Teams, SMS, and DMs. It overrides the general tell catalogue where necessary.

## What to preserve

Lowercase starts, fragments, contractions, ellipses, tone emoji, reaction replies, a follow-up correction, and mild slang can all be natural in an established thread. Do not "correct" them out of context.

## Avoid

Avoid headings or lists for one-line answers; corporate acknowledgements; service closers; announcing a message instead of writing it; restating the question; and a recap in a message readable at a glance.

## Markup

| Surface | Bold | Italic | Code | Lists | Headings |
|---|---|---|---|---|---|
| WhatsApp | `*bold*` | `_italic_` | fenced or inline | Newlines only | None |
| Slack mrkdwn | `*bold*` | `_italic_` | fenced or inline | `•` or `-` | Bold line |
| Discord | `**bold**` | `*italic*` | fenced or inline | `-` | `#` supported |
| Teams | `**bold**` | `*italic*` | fenced | `-` | Limited |
| SMS | None | None | None | None | None |

Use Slack links as `<https://url|label>`. Preserve existing mentions and never invent one. Do not emit Markdown headings or tables into WhatsApp, Slack, or SMS.

## Length and register

Match the recent thread before choosing language, formality, or French `tu` versus `vous`. Send one idea per message. Over roughly five WhatsApp lines, consider whether a call, document, or thread is a better home and say so. Put the answer first in a Slack channel or thread, then detail.
