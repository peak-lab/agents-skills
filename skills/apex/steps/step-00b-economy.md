---
name: step-00b-economy
description: Disable nested delegation while preserving the normal quality bar
load_condition: economy_mode = true
returns_to: step-00-init.md
---

# Optional setup: economy

Set `{delegation_allowed}` to `false` and return to `step-00-init.md`.

Economy mode changes orchestration only. Use direct repository and documentation tools, but keep the same scope, acceptance criteria, safety rules, and proportionate verification. Do not add economy banners or reduce tests by an arbitrary count.
