# Provenance

Adapted from the locally installed PeakLab `humanize` skill on 2026-09-12. Its required text references are bundled in this package so the skill has no dependency on a user's installed configuration.
